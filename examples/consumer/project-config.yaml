apiVersion: platform.example.io/v1alpha1
kind: Project
metadata:
  name: storefront
  namespace: platforms
spec:
  owner: platform-team
  # stage and replicas are defaulted locally from the CRD before rendering.
